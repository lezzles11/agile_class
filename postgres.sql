CREATE TABLE users(
	fullName VARCHAR(64) NOT NULL, 
	email VARCHAR(64) NOT NULL, 
	password VARCHAR(64) NOT NULL,
	country VARCHAR(32) NOT NULL, 
	mailingAddress VARCHAR(128),
	contactNumber VARCHAR(32), 
	courses VARCHAR(64),
	paymentInfo VARCHAR(128)
); 



